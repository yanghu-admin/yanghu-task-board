# 移交缺口核查报告

> 核查方：WorkBuddy
> 核查对象：《项目移交文档_Marvis到WorkBuddy_20260723.md》及本工作区实际文件
> 核查日期：2026-07-23
> 工作区：`D:\workbuddy\养护看板`
> 结论一句话：**文件基本齐全，但"说明书"严重失真——数据流写漏、版本状态记错、凭据状态报反，且存在明文密钥安全隐患。**

---

## 核查方法

1. 通读移交文档全文，逐项对照 §3 文档清单与实际目录（根目录 61 项 + `output/` 8 项 + `规范库/` 4 项 + `baselines/` 3 项 + `images/`）。
2. 读取关键源码：`index_live.html`、`feishu_sync_form.py`、`feishu_sync_review.py`、`wework_bot_relay.py`、`README.md`、`marvis_指令.md`。
3. 用移交文档提供的 GitHub Token **只读**核验线上仓库真实状态（未做任何写操作）。
4. 比对本地文件字节数 / 行数 / SHA 与文档记录。

---

## 一、🔴 严重：文档与事实不符（会直接误导后续开发）

### 缺口 1：飞书凭据并非"占位符"，而是真实已配置

| 项 | 移交文档说法（§1.4 / §6.1） | 实际情况 |
|----|------------------------------|----------|
| 飞书 App ID | `YOUR_FEISHU_APP_ID`（未配置） | `cli_aab0cfc248b9dcfa`（真实值，写在两个 py 脚本中） |
| 飞书 App Secret | `YOUR_FEISHU_APP_SECRET`（未配置） | 真实值已硬编码在脚本中 |
| 多维表格 App Token | `YOUR_FEISHU_APP_TOKEN`（未配置） | `EwJqbhQMbaU3XhsnTJ7cHcgFn7g`（真实值） |
| 数据表 | 单表设想 | 实为**三张真表**：主表 `tbljdl99HjJrwiSs`、核验提交表 `tblBh8mjI3BiDLmh`、经办人提交表 `tblI0tKOoMy4fyX6` |

**风险**：若照文档判定"飞书没配好、可忽略"，会整体漏掉正在运行的数据中台链路。
**需 Marvis 解释**：这套飞书应用/表格是否仍有效在用？为何文档要写成占位符？

---

### 缺口 2：真实数据流被漏写（核心同步环节完全缺失）

移交文档 §2.2 架构图只画了「企业微信 → 中转 → 飞书 → HTML看板」。实际链路（从源码还原）为：

```
① 企业微信群消息 → wework_bot_relay.py(:5001) → 飞书主表(建单)
② 经办人飞书表单提交       → 飞书「经办人提交表」tblI0tKOoMy4fyX6
③ 核验人（黄瑾文/邹佳飞）   → 飞书「核验提交表」tblBh8mjI3BiDLmh
        ↓
④ feishu_sync_form.py + feishu_sync_review.py   ← 移交文档只字未提
   （拉飞书三表 → 回写飞书主表 → 同步到本地 data.json，重算 statistics）
        ↓
⑤ data.json  ← index.html 通过 fetch('data.json') 渲染看板
        ↓
⑥ git push / REST API → GitHub Pages 刷新
```

**漏写内容**：
- `feishu_sync_form.py`（经办人提交同步：受理/维修中/维修后照片 → 状态流转）
- `feishu_sync_review.py`（两级核验同步：一级黄瑾文、二级邹佳飞）
- 飞书中文字段 ↔ data.json 英文字段的映射规则（仅散落在脚本里，无独立文档）
- §6.2 的"工单字段设计（桩号/病害类型/紧急程度…）"是设想稿，与真实表字段（工单号/状态/一级核验/二级核验/维修前中后照片/受理时间…）对不上

**需 Marvis 解释**：真实字段映射表；两个同步脚本的触发方式（手动 / 定时 / 计划任务？）。

---

### 缺口 3：线上版本 SHA 与行数记录全部失效

| 项 | 移交文档说法 | 只读核验的实际值 |
|----|--------------|------------------|
| 当前线上 index.html SHA | `58a96397c160…`（"回退后干净版"） | **`10dd876361fc`** |
| 线上 index.html 大小 | 约 138KB / "干净版无工单" | **141,329 字节** |
| 线上文件与哪个本地文件一致 | 未明确 | **与本地 `index_live.html`（141,329B）逐字节一致** |
| index_live.html 行数 | 4076 / 4686 行 | **实际 3485 行** |
| 本地 index.html | （被当作线上版） | 122,652 字节 / 2993 行，是**旧/另一版**，并非线上版 |

**结论**：真正的线上版就是 `index_live.html`；文档记录的 SHA、大小、行数、版本归属均已过期或错误。
**需 Marvis 解释**：`index.html`（旧，122KB）与 `index_live.html`（线上，141KB）的关系；工单模块到底在不在线上版里。

---

### 缺口 4：部署方法三份文档三种口径

| 来源 | 部署方式 |
|------|----------|
| 移交文档 §1.2 / §5.2 | PowerShell 二进制读 + Base64 + REST API PUT |
| README.md §一.2 | 本地更新 → `git push` → Pages 自动刷新 |
| marvis_指令.md §一 | `git add -A && git commit && git push` |

**风险**：接手者不知以哪套为准。
**需 Marvis 解释**：当前实际生效的是哪一种？为什么并存两套？

---

## 二、🟡 信息缺口（该写但未写）

| 编号 | 缺口 | 影响 |
|------|------|------|
| I-1 | **8人权限机制未说明** | 文档称"8人角色权限系统"，实际是 `index_live.html` 内的 `?share=` 分享令牌 + 单一 admin 密钥，且"任意密钥可作普通员工登录"。非严谨 RBAC。缺：admin 密钥值、8人分享链接如何生成分发、鉴权边界。 |
| I-2 | **同步脚本无运行说明** | `feishu_sync_form.py` / `feishu_sync_review.py` / `wework_bot_relay.py` 均缺：Python 版本、依赖（无 `requirements.txt`，需 requests/flask）、启动命令、触发/定时方式。 |
| I-3 | **字段映射无独立文档** | 飞书中文字段 ↔ data.json 英文字段的映射只藏在脚本代码里。 |
| I-4 | **README.md 已过期** | 记录的是 7/19 旧流程（"转发给 Marvis" + 金数据表单），与新架构冲突，会误导。 |
| I-5 | **路径全部指向 output/** | 移交文档 §10 建议阅读路径全写 `output/xxx`，但文件实际在根目录；`output/` 仅 8 个文件。照文档找会找不到。 |
| I-6 | **存在未登记的现有文件** | `Marvis检讨书_20260723.md`、`baselines/`（7/20 快照 3 份）、`relay.log`、`feishu_sync_*.py`、`feishu_sync_form.py` 等在库但清单未列，用途不明。 |
| I-7 | **中转服务 API Key 与飞书链路的衔接** | 企业微信 API Key `WG8u01VHnECh3BpdvSXti4fDTKN26Ocb` 在文档中出现，但 `wework_bot_relay.py` 是否真正写入飞书主表、字段如何对应，需与 §缺口2 一并确认。 |

---

## 三、🛑 安全隐患（建议尽快处理）

| 编号 | 隐患 | 位置 | 建议 |
|------|------|------|------|
| S-1 | **GitHub PAT 明文** | 移交文档 §1.1 / §1.2（`ghp_...`） | 该文档已被复制/多方查阅，**建议立即轮换 Token**；改用环境变量。 |
| S-2 | **飞书 APP_SECRET 明文** | `feishu_sync_form.py:13`、`feishu_sync_review.py:10` | 移入环境变量 / 本地 `.env`（不进 git）；条件允许则轮换。 |
| S-3 | **企业微信 API Key 明文** | 移交文档 §1.3、中转脚本 | 同上，纳入统一密钥管理。 |

---

## 四、✅ 已确认齐全的部分

- 移交清单 §3.1 列出的核心文档（阶段日志、同行评议材料、规范库、中转脚本、工单预览、账号清单、各类方案文档）**绝大多数在根目录可找到**，仅个别文件名略有出入（如"三级愿景"vs"三项愿景"）。
- `规范库/`（规范清单/目录索引/病害规范映射表.json/维修方案模板）完整。
- `data.json`（本地 7,435B）与线上 `data.json`（7,435B、SHA `ea5c9db5a2d0`）一致。
- `baselines/` 保留了 7/20 的 index.html 与 data.json 快照，可作回退参考。
- **判断：不是"缺文件"，而是"文件在、说明书失真"。**

---

## 五、待 Marvis 澄清的问题清单（请逐条回答）

1. 飞书应用（`cli_aab0cfc248b9dcfa`）与三张表是否仍有效在用？为何移交文档写成占位符？
2. `feishu_sync_form.py` / `feishu_sync_review.py` 的真实触发方式？（手动运行 / 定时任务 / 其他）
3. 飞书表字段 ↔ data.json 字段的完整映射表？
4. 线上版到底是不是 `index_live.html`？本地 `index.html`（旧版）还有用吗？工单模块是否已在线上？
5. 当前生效的部署方式是"git push"还是"REST API PUT"？为何两套并存？
6. 8人权限：admin 密钥值是什么？8 人的分享链接如何生成与分发？
7. `wework_bot_relay.py` 当前是否真在运行？部署在哪台机器/什么穿透方式？
8. `Marvis检讨书_20260723.md`、`baselines/`、`relay.log` 等未登记文件的用途？

---

## 六、WorkBuddy 建议的下一步（待用户确认）

1. **优先**：轮换已暴露的 GitHub PAT / 飞书 APP_SECRET / 企业微信 API Key，脚本改读环境变量。
2. 待 Marvis 回答上述 8 问后，由 WorkBuddy 重写一份"真实版交接补遗"，替换失真内容。
3. 校准线上版本基线（明确 `index_live.html` = 线上版），归档旧 `index.html`。
4. 为三个脚本补 `requirements.txt` + 运行/定时说明，形成可一键复现的运行手册。

*（本报告由 WorkBuddy 核查生成，所有线上核验均为只读操作，未改动任何线上内容。）*
