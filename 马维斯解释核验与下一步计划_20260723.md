# 马维斯解释核验与下一步计划

> 核查方：WorkBuddy
> 日期：2026-07-23 11:56
> 前置文档：《移交缺口核查报告_20260723.md》
> 本文档：对马维斯就 8 个问题的答复逐条核验（能验证的都在本机实测了），并给出下一步计划。

---

## 一、逐条核验结果

| 问题 | 马维斯答复 | 实测核验 | 判定 |
|------|-----------|---------|------|
| 1. 飞书凭据 | 审计遗漏导致误写占位符，实际已配置 | 与源码一致 | ✅ 解释成立 |
| 2. 同步脚本触发 | 手动执行 + 「已同步」字段幂等 | 与源码一致（无调度逻辑、处理完 exit） | ✅ 成立 |
| 3. 字段映射 | 给出两张映射表 | 与脚本源码比对一致（细节：维修中/维修后照片合并进主表`照片`与 `images.after`） | ✅ 成立 |
| 4. 线上版基线 | 线上 = `index_live.html`，旧 `index.html` 应归档 | 与逐字节比对结论一致 | ✅ 成立 |
| 5. 部署方式 | **"当前机器从未安装 Git"**，故用 API PUT | ❌ **本机 Git 2.54.0 已安装**，且项目目录就是一个 git 仓库（remote 指向 yanghu-task-board，最后本地提交 `e36a07c`） | ❌ **不成立** |
| 6. 8人权限 | 分享令牌 + admin 密钥，密钥需从源码提取 | 已提取：`ADMIN_KEY='yh2026'`；发布者密钥 `hjw2026`=黄瑾文、`cxh2026`=陈晓辉；其余任意密钥=普通员工 | ✅ 成立（但见安全新增项） |
| 7. relay 服务状态 | 大概率未运行，需 netstat 确认 | **半对半错**：Flask relay 确实未运行；但 **5001 端口已被占用**——是 Marvis 运行时残留的静态文件服务器（见下） | ⚠️ 部分成立 |
| 8. 未登记文件用途 | 逐一说明 | 合理，无矛盾 | ✅ 成立 |

---

## 二、本次核验的三个新发现

### 新发现 1：本机 Git 可用，且本地 git 仓库与线上已"分叉" 🔴

```
git version 2.54.0.windows.1
remote: https://ghp_***@github.com/yanghu-admin/yanghu-task-board.git
本地最后提交: e36a07c "fix: 修复 marvis 指令格式"
工作区状态: data.json / index.html / 图片 已修改未提交；数十个文档未跟踪
```

**要害**：线上 `index.html` 后来是通过 REST API PUT 直传更新的（内容=index_live.html），**没有经过本地 git**。所以本地 git 历史落后于线上真实内容。
→ **此时如果直接 `git push`（甚至 force push），会把线上新版覆盖回旧版**。必须先 `git fetch` 对齐远端、确立 `index_live.html` 为唯一基线后再恢复 git 工作流。

### 新发现 2：5001 端口被残留静态服务器占用，且整个项目目录暴露在局域网 🔴

```
PID 24428: "C:\Program Files\Tencent\Marvis\MarvisAgent\...\python311\python.exe"
           -m http.server 5001 -d D:\workbuddy\养护看板
监听: 0.0.0.0:5001（含 http://192.168.3.221:5001）
```

这是 Marvis 运行时起的**目录级静态文件服务器**，不是 Flask relay。问题：
1. **整个项目文件夹（含明文密钥的 py 脚本、移交文档里的 GitHub PAT）正对局域网可读**；
2. 它占着 5001，将来 relay 想启动也起不来。
→ 建议尽快停掉该进程（PID 24428）。

### 新发现 3：admin 密钥是"公开的" 🟡

`ADMIN_KEY='yh2026'` 等密钥硬编码在 `index_live.html` 里，而这个文件就是 GitHub Pages 上的公开页面——**任何人查看网页源码都能拿到管理员密钥**。短期内可接受（内部小工具），但要知道这层"权限"只防君子。

### 附：密钥暴露面比之前统计的更大

GitHub PAT 除了写在移交文档里，还**嵌在 git remote URL 里**（`.git/config`）。轮换 PAT 后记得同步更新 remote。

---

## 三、对马维斯答复的总体评价

- 8 问中 6 问解释成立且与源码一致，字段映射表质量高，可直接并入运行手册；
- 但"本机没有 Git"是错的（也可能是他当时的执行环境与本机不同，无从考证）；
- "5001 需要现场确认"算是诚实，但残留静态服务器这种安全问题他并不知情。
- **教训再次验证：一切以源码和本机实测为准，文档（包括解释）只作参考。**

---

## 四、下一步计划（建议执行顺序）

### P0 — 安全止血（半小时内可做完）
| # | 动作 | 说明 |
|---|------|------|
| 1 | **停掉 PID 24428 静态服务器** | 消除项目目录局域网暴露 + 释放 5001 |
| 2 | **轮换 GitHub PAT** | 需用户在 GitHub 后台操作；新 PAT 不再写入文档，改用环境变量 + 更新 git remote |
| 3 | **轮换飞书 App Secret** | 需管理员在飞书开放平台操作；脚本改读环境变量 |
| 4 | 企业微信 API Key 一并纳管 | relay 未上线，优先级略低 |

### P1 — 基线校准（WorkBuddy 可直接做）
| # | 动作 | 说明 |
|---|------|------|
| 5 | `git fetch` 对齐远端，确认线上内容进入本地历史 | 防止误推覆盖 |
| 6 | 确立 `index_live.html` = 唯一开发基线；旧 `index.html` 归档到 `archive/` | 消除双版本混乱 |
| 7 | 恢复标准 git push 工作流，弃用 API PUT | 上线前仍须用户审核（遵守"不私自部署"红线） |

### P2 — 文档与可复现性
| # | 动作 | 说明 |
|---|------|------|
| 8 | 撰写《交接补遗_真实架构与运行手册》 | 真实数据流 + 字段映射（用马维斯答复3）+ 三脚本运行方法 + 密钥管理规范 |
| 9 | 补 `requirements.txt`（requests、flask） | 一键装依赖 |
| 10 | 更新过期 README | 与真实流程对齐 |

### P3 — 之后再谈
- 智能派单模块重启（基于 v2 SHA `7d0b635` 的代码考古）
- relay 上线（穿透方案 + 守护进程）
- 权限方案是否升级（当前明文密钥模式的取舍）

---

*（所有核验均为只读操作；停进程、轮换密钥等动作待用户确认后执行。）*
