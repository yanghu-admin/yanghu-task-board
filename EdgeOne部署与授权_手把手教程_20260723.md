# EdgeOne Pages 部署与授权 · 手把手教程（给老板）

> 目标：把养护看板搬到腾讯云 EdgeOne Pages。以后一线人员在网页上**选照片 + 填桩号 + 提交**，数据自动写进 GitHub 仓库并立刻上线，**你不用碰服务器、不用跑脚本、不用 git**。
> 你只需做两类"点几下"的操作：**① 换一个 GitHub 令牌；② 在腾讯云点几下连仓库**。其余代码和部署都由 WorkBuddy 完成。

---

## 第 0 步（必须）：先把旧的 GitHub 令牌作废、换新的

旧令牌 `ghp_oGeU8lF7ZbOq...` 之前明文泄露过，必须先换。步骤见《密钥轮换_手把手教程_20260723.md》里的 **GitHub 部分**（打开 github.com → 头像 → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token）。

- 权限只勾 **`repo`**（全选 repo 那一组即可）。
- 生成后，**把那段 `ghp_...` 字符串完整复制下来，发给我**（WorkBuddy）。
- 在 GitHub 里把**旧令牌点 Revoke 作废**。
- ⚠️ 飞书那边：按之前结论，飞书应用直接停用即可，**不用**轮换飞书密钥。

---

## 第 1 步：登录腾讯云 EdgeOne（约 1 分钟）

1. 打开 👉 **https://console.cloud.tencent.com/edgeone**
2. 用你的**微信或 QQ**扫码登录（第一次会让你开通，按提示点"开通"即可，免费额度够用）。
3. 左侧菜单找到 **「EdgeOne Pages」**（或叫"Pages 服务"），点进去。

---

## 第 2 步：绑定你的 GitHub（授权一次，约 1 分钟）

1. 在 EdgeOne Pages 页面点 **「绑定 GitHub」/「授权 GitHub」**。
2. 会跳到 GitHub 登录页 → 点 **Authorize**（授权 EdgeOne 访问你的仓库）。
3. 授权时**建议只勾选 `yanghu-task-board` 这一个仓库**（更私密）。
4. 授权成功后会跳回 EdgeOne。

---

## 第 3 步：创建项目（填这 5 项，约 2 分钟）

1. 点 **「创建项目」→「从 Git 仓库创建」**。
2. 选仓库：**`yanghu-admin/yanghu-task-board`**。
3. 分支：**`master`**。
4. 构建命令填：**`npm run build`**
5. 输出目录填：**`dist`**
6. 点「保存配置 / 创建」。

> 说明：代码我已经帮你改好并会推到 GitHub（含网页和边缘函数）。你创建项目后，EdgeOne 会自动拉代码构建。

---

## 第 4 步：设置环境变量（最关键，约 1 分钟）

在刚创建的项目里，找到 **「环境变量」/「Environment Variables」** 设置页，添加 4 条：

| 变量名 | 值 | 说明 |
|------|------|------|
| `GITHUB_PAT` | 你第 0 步生成的新 `ghp_...` 令牌 | 让边缘函数有权写仓库 |
| `GH_OWNER` | `yanghu-admin` | 仓库所有者（固定） |
| `GH_REPO` | `yanghu-task-board` | 仓库名（固定） |
| `GH_BRANCH` | `master` | 分支（固定） |

填完点「保存」。

---

## 第 5 步：部署（点一下）

1. 回到项目页，点 **「部署」/「重新部署」**。
2. 等 1–3 分钟，状态变成 **「成功」**。
3. EdgeOne 会给你一个网址，形如 **`https://xxxx.edgeone.site`**（也可在设置里绑你自己的域名，先不管）。

---

## 第 6 步：验证（确认自动发布跑通）

1. 打开你的 EdgeOne 网址。
2. 点「+ 新建任务」→ 选一张现场照片 → 填桩号（或描述里写"K15+600 护栏坏了"会自动提取）→ 填创建人 → 提交。
3. 提交后页面会自动刷新，新任务出现；**同时**打开 `https://github.com/yanghu-admin/yanghu-task-board` 的 `data.json`，会看到新任务已写进去。
4. 让另一个人用手机打开同一网址，也能看到这条任务 → **多端实时同步闭环成立**。

---

## 出问题怎么办

- **网页打不开 / 空白**：把 EdgeOne 给的网址发我，我查日志。
- **提交任务报错"提交失败"**：多半是 `GITHUB_PAT` 没填对或权限不够（必须含 `repo`），或令牌已过期；重填后点「重新部署」。
- **照片不显示**：照片走 GitHub 实时地址，需等 1–2 秒；若一直不显示，把网址发我。

---

## 你总共要做的动作（极简清单）

1. ☐ 按《密钥轮换》教程：GitHub 生成新 PAT、发我、作废旧的。
2. ☐ 登录 EdgeOne（微信扫码）→ 绑定 GitHub → 创建项目（填 5 项）→ 设 4 个环境变量 → 点部署。
3. ☐ 把 EdgeOne 网址发我，我帮你验证。

**代码、函数、推送、上线验证，全部由 WorkBuddy 完成。你不用懂 git、不用敲命令。**
