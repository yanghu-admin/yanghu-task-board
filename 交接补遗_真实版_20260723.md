# 交接补遗 · 真实版（2026-07-23）

> 配套文件：`移交缺口核查报告_20260723.md`、`马维斯解释核验与下一步计划_20260723.md`
> 本文用于**纠正** `项目移交文档_Marvis到WorkBuddy_20260723.md` 中的失真描述。
> 原移交文档整体框架可用，但以下关键处与事实相反或缺失，以本文件为准。

---

## 0. 一句话结论

**不是缺文件，是说明书失真。** 核心代码与文档基本齐全，问题在于：飞书凭据被误写成占位符、真实数据流被漏写、线上版本基线记录错误、部署方式口径不一。本文逐项更正。

---

## 1. 飞书凭据：已配置，非占位符

原文档 §1.4、§6.1 写「飞书 App ID / Secret / Token 均为占位符，尚未配置」——**与事实相反**。

真实情况（来自 `feishu_sync_form.py` / `feishu_sync_review.py` 源码，已脱敏迁移至 `.env`）：

| 项 | 值 |
|----|----|
| 飞书应用 App ID | `cli_aab0cfc248b9dcfa` |
| 应用令牌 app_token | 见 `.env.example`（即飞书 base 地址中的一串） |
| 主表 table_id | `tbljdl99HjJrwiSs` |
| 经办人提交表 | `tblI0tKOoMy4fyX6` |
| 核验提交表 | `tblBh8mjI3BiDLmh` |

**后果**：若信了「飞书未配置」，会漏掉整条正在运行的数据链路。

---

## 2. 真实数据流（原文档完全漏写）

原架构图只画了「企业微信 → 中转 → 飞书 → HTML 看板」，但缺了最关键的一环：**飞书 → data.json 的同步脚本**。

```
企业微信群消息
   └─ wework_bot_relay.py (Flask :5001) ──► 飞书主表（自动建单）
经办人飞书填「经办人提交表」
核验人飞书填「核验提交表」
   └─ feishu_sync_form.py   （手动执行）
   └─ feishu_sync_review.py （手动执行）
            ▼
      本地 data.json  ◄── index.html 用 fetch('data.json') 读取
            ▼  git push (master)
      GitHub Pages 发布看板
```

- 两个同步脚本**不在**原移交清单里，但它是数据从飞书落到看板的必要环节。
- 原文档 §6.2 的「工单字段设计（桩号/病害类型/紧急程度…）」是设想稿，与真实表字段（工单号/状态/一级核验/二级核验/维修前后照片…）不符。

---

## 3. 同步脚本的触发方式

两个 `feishu_sync_*.py` 均为**手动执行**：
- 无定时调度（无 schedule / while True / cron）；
- 每次处理完积压未同步记录后 `exit()`；
- 用飞书表「已同步」布尔字段做幂等标记，可重复运行。

当前运行方式：有人提交经办/核验表单后，由人手动跑对应脚本。

---

## 4. 飞书字段 ↔ data.json 字段映射

### feishu_sync_form.py（经办人提交 → 主表 → data.json）

| 飞书经办人提交表字段 | 飞书主表字段 | data.json 字段 |
|---|---|---|
| `工单号(自动)` | `工单号`（匹配） | `task.id` / `task.number` |
| `维修前照片` | — | `task.images.before` |
| `维修中照片` / `维修后照片` | `照片`（合并） | `task.images.after` |
| `备注` | `问题描述` | `task.notes` |
| — | `状态`→`处理中`/`待核验` | `task.status`→`processing`/`pending_review` |
| — | `受理时间`（毫秒时间戳） | `task.accepted_at` |
| — | — | `task.completed_at`（维修后照片到则设置） |

### feishu_sync_review.py（核验提交 → 主表 → data.json）

| 飞书核验提交表字段 | 飞书主表字段 | data.json 字段 |
|---|---|---|
| `工单号` | `工单号`（匹配） | `task.id`（匹配） |
| `核验级别`（一级/二级） | 路由到 `一级核验`/`二级核验` | `task.reviews.level1`/`level2` |
| `核验结果`（通过/不通过） | `一级核验`=`已通过/退回` 等 | `result: approved/rejected` |
| `备注` | `问题描述`（不通过时） | `comment` |
| `验收照片` | — | `images` |
| — | `状态`→`需整改`/`已完成` | `task.status`→`rejected`/`completed` |

---

## 5. 线上版本基线（原文档记录错误）

原文档称「线上 SHA = `58a96397…`、线上是干净版」——**错误**。

实测（GitHub API + 实时站点双重验证）：
- **线上 `index.html` = 141,329 字节，sha `10dd876361fc…`**，与本地 `index_live.html` 一字不差。
- 本地旧 `index.html`（122,652 字节）是工单模块合并前的旧版，已归档为 `archive/index_20260723_old.html`。
- **唯一正确基线 = `index.html`（即原 `index_live.html`）**。原 `index_live.html` 已合并重命名为 `index.html`。

---

## 6. 部署方式（统一为 git push）

原文档（§1.2/§5.2）写「PowerShell 二进制读 + Base64 + REST API PUT」；README/marvis_指令写「git push」。两种口径。

真相与处理：
- 本机**已安装 Git**（原「无 Git 故用 API PUT」说法不成立），应统一为 `git push`。
- API PUT 仅作应急保留。
- **推送前必须先 `git fetch` 对齐远端**：线上 `index.html` 曾由 API PUT 直传，本地 git 历史一度落后，直接 push 会覆盖线上版本。

---

## 7. 8 人权限机制（非严格 RBAC）

原文档称「8 人角色权限系统」，实际是极简分享令牌模式（在 `index.html` 中）：
- 看板通过 `?share=` 参数生成分享链接，持有链接即可访问；
- 单一 admin 密钥做管理员入口；
- 任意非空密钥可进入「普通员工」视图。

**已知弱点**：admin 密钥（如 `ADMIN_KEY='yh2026'`）硬编码在公开 GitHub Pages 源码中，任何看源码者均可获取。生产环境应改为服务端校验。

---

## 8. 密钥管理（已做 + 待做）

已完成（WorkBuddy 接管后）：
- 新建 `.env.example`（模板）、`.gitignore`（排除 `.env`/日志/缓存）、`env_loader.py`（共享加载器）。
- `feishu_sync_form.py` / `feishu_sync_review.py` 改为 `from env_loader import ...`，移除明文密钥。
- 本地 `.env` 已建（gitignored），含当前真实值。
- 原移交文档与阶段日志中的明文 PAT、飞书 App Secret 已脱敏为占位符。

待老板手动操作（涉及第三方平台）：
- **P0-2 GitHub**：轮换 PAT（Settings → Developer settings → Tokens），更新 `.env` 的 `GITHUB_PAT`；旧 PAT `ghp_oGeU8lF7ZbOq…` 已泄露。
- **P0-3 飞书**：轮换 App Secret（开发者后台 → 应用 `cli_aab0cfc248b9dcfa` → 凭证与基础信息 → 重置），更新 `.env` 的 `FEISHU_APP_SECRET`。
- **P0-4 企业微信**：轮换 API Key（优先级可稍后）。

---

## 9. 中继服务（wework_bot_relay.py）

- Flask 服务，端口 5001，需公网可达 + 企业微信机器人回调指向该地址 + 进程持续运行。
- 当前**未配置开机自启/守护进程**，需手动 `python wework_bot_relay.py` 启动。
- 部署状态用 `netstat -an | findstr 5001` 在运行机器上确认。

---

## 10. 未登记文件用途（原清单未列）

| 文件 | 用途 |
|------|------|
| `Marvis检讨书_20260723.md` | Marvis 停职前最后产物 |
| `baselines/` | 7/20 的 data.json / index.html / 飞书表结构快照 |
| `relay.log` | wework_bot_relay.py 运行时日志（测试残留，已 gitignore） |
| `feishu_sync_form.py` / `feishu_sync_review.py` | 见 §2-§4 |
| `photo_watermark.py` | 照片水印工具 |
| `sync_cloud.py` | 云端同步辅助脚本（在远端仓库，不在本地工作区） |

---

*本文档取代原移交文档中上述失真描述，作为接手后的权威参考。*
